# [2026-09-13] 账本加宽（V0.15.3）

- 类型：功能修改（布局）
- 上线版本：V0.15.3
- 状态：✅ 已完成并验证

## 需求与改动

用户："我觉得账单的宽度可以变宽一点。" 内容区被 `main { max-width: 920px }` 居中封顶。

**一行修复**：`main { max-width: 1200px }`（保留上限，超宽屏不会无限拉伸）。

## 验证（数值 + 截图）

- 账页宽 **816 → 910px**（main 1014px，视口 1256）；
- **附带收益**：记录表此前因页面不够宽，"操作"列（编辑/删除）被裁掉需横向滚动；
  加宽后表格 `869/869` **完整放下、横向滚动消失**；
- 截图确认：日期到"删除"全列完整可见；零报错 ✓

## 涉及文件

`css/style.css`（main max-width）。
