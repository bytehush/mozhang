# [2026-09-19] 开源发布 GitHub

- 类型：新功能开发（仓库级）
- 上线版本：V0.15.5（无代码功能变更）
- 状态：✅ 已完成并验证

## 需求描述

用户决定将墨账开源，发布到 GitHub 公开仓库（`bytehush/mozhang`），让任何人可以下载、使用、参与。

## 开源前处理

1. **敏感与隐私排查**：全库扫描无硬编码密钥/口令（AI Key 一贯只存 localStorage、由用户自填）
2. **`.gitignore` 扩充**，三类内容排除在公开仓库外（均保留在本地）：
   - `.mimosa/` —— 本地 AI 工具状态（安全扫描/钩子缓存），并执行 `git rm -r --cached` 停止跟踪
   - `打包发布/` —— 便携分享包等构建产物
   - `项目复盘/` —— 个人开发复盘，**含简历与面试口径等求职资料，不公开**
3. **新增开源门面文件**：
   - `README.md` —— 项目介绍、功能亮点、快速开始、技术栈、目录结构、文档体系、测试
   - `LICENSE` —— MIT 协议
4. 开源前把工作区中 V0.15.4/V0.15.5 的成果先行提交（系统直读兜底卡、云端 AI 接入点、便携分享版文档）

## 验证结果

- `node --check js/chat.js js/cloud-config.js cloudfunctions/mozhang-ai/index.js` 全部通过
- 敏感信息 grep 扫描零命中
- 推送后核对 GitHub 仓库文件清单：无 `.mimosa/`、无打包产物、无个人复盘文档

## 影响范围与后续计划

- 不影响任何运行时功能；`AGENTS.md` 保留在仓库中（作为 AI 结对开发的协作指南，也是项目特色）
- 后续可考虑：Release 页挂便携包 zip、补充界面截图到 README、GitHub Actions 做语法检查
