# 记账本 · 使用说明

这是一个在你自己电脑上运行的记账工具。V0.6 起支持**多账本**：内置「计时工 / 计件工 / 通用收支」三种记账模板，可以同时记好几种账，用图表看趋势，还能让 AI 帮你分析。

---

## 〇、多账本怎么用（V0.6 新增）

- 左侧边栏顶部是**账本切换器**：点开能看所有账本（各有多少条记录），点谁就切到谁的账
- **新建账本**：切换器菜单里点「＋ 新建账本」→ 选模板（计时工/计件工/通用收支）→ 起名 → 创建
- **账本设置**：菜单里点「账本设置」可改名、改默认时薪/单价，也能删除账本（会连记录一起删，先备份！）
- 每本账的视图、筛选偏好各自记忆，互不影响
- 旧数据已自动迁入「服装厂计时工」账本（原始数据留存备份，导出备份文件也升级为包含全部账本，旧备份文件仍可导入）

---

## 一、怎么打开（每天用就这一步）

1. 双击文件夹里的 **「启动记账APP.bat」**
2. 会自动弹出浏览器打开应用（地址是 `http://127.0.0.1:8787`）
3. 用完后直接关掉那个黑色命令行窗口即可

> 💡 想让桌面有个正式图标：右键「启动记账APP.bat」→ 发送到 → 桌面快捷方式；
> 再右键桌面快捷方式 → 属性 → 更改图标 → 浏览选中项目文件夹里的 **app-icon.ico** 即可。
> ⚠️ 注意：双击 bat 后弹出的**黑色窗口不要关**（关了应用就停了），可以把它最小化。
> 如果提示"node 不是内部或外部命令"，说明电脑没装 Node.js，去 https://nodejs.org 下载安装（一路点下一步即可），装完再双击 bat。

## 二、第一次使用：设置时薪

打开左侧账本切换器 → 点「账本设置」：

| 设置项 | 说明 | 示例 |
|---|---|---|
| 账本名称 | 随便起，比如"张姐的计时工账" | |
| 正常时薪 | 正常上班每小时的钱 | 20 元 |
| 加班时薪 | 加班每小时的钱 | 30 元 |

改完点「保存账本设置」。时薪只是**新记录的默认值**，每天记录时还能单独改。

## 三、每天怎么记账（点时间工记录法）

在「记录表」页点「✒️ 记录」（悬浮窗里填）：

1. 选好**日期**（默认今天）
2. 填**正常班**的开始、结束时间（比如 08:00 ~ 17:00）
3. 如果今天加班，勾选「**今天有加班**」，再填加班起止时间（比如 18:00 ~ 21:00）
4. 下面会**实时显示**：正常工时、加班工时、当天工资多少钱
5. 点「保存记录」

填错不怕：在「📋 记录表」里每条记录后面都有**编辑**和**删除**按钮。

> 上班时间跨半夜也不用管（比如 20:00 上到第二天 04:00），软件会自动按跨天计算。

## 四、看图表

点「📈 图表」，有两张图：

- **每日工资走势**（折线图）：绿色线是当天总工资，蓝色虚线是正常班工资，橙色虚线是加班费
- **每日工作时间**（折线+条形图）：蓝色条形是正常工时，橙色条形是加班工时（叠在上面），绿色折线是当天总时长

右上角可以切换看「最近 7 条 / 14 条 / 30 条 / 全部」。

## 五、AI 分析报告（免费）

先做一次性准备（只需做一次）：

1. 浏览器打开 **open.bigmodel.cn**（智谱开放平台）
2. 注册/登录（手机号即可）
3. 右上角头像 → 进入「**控制台**」→ 左侧菜单「**API Keys**」
4. 点「创建 API Key」，复制那一串字符
5. 回到记账应用「⚙️ 设置」→ 粘贴到 **API Key** 框 → 保存

以后每次：点「🤖 AI 助手」→ 点「**生成分析报告**」，等十几秒，AI 会生成一份报告，包括：

- 工资和工时的**趋势分析**（在涨还是在跌、哪天最累、星期几加班最多）
- **工作状态建议**（怎么安排节奏更高效）
- **健康建议**（睡眠、护眼、颈肩腰椎——特别针对缝纫久坐的特点）
- **过劳风险提示**（连续加班太多会直接点名）
- **收入优化建议** + **下阶段行动清单**

报告中所有结论都会引用你的真实数字，不是空话。

## 六、数据安全

- 所有数据**只存在你这台电脑的浏览器里**，不会上传到任何服务器
- 建议**定期备份**：设置 → 「⬇ 导出备份」，会下载一个 json 文件，收好它
- 换电脑/换浏览器：在新环境里用「⬆ 导入备份」选之前导出的文件即可

## 七、常见问题

| 问题 | 解决办法 |
|---|---|
| 打不开页面 | 检查黑色命令行窗口是否还开着；看窗口里显示的端口（8787 被占用时会自动换成 8788 等） |
| AI 报"API Key 无效" | 去 open.bigmodel.cn 检查 Key 是否复制完整，重新粘贴保存 |
| 记错了想改 | 记录表 → 该行「编辑」 |
| 想重新开始 | 设置 → 「清空全部数据」（建议先导出备份） |
| 想快速看效果 | 设置 → 「填入示例数据」，随时可清空 |

---

*后续版本计划：支持更多记录方式（如计件工资）——等你告诉我具体规则后迭代。*
